/**
 * @s2gold/engine — deterministic, DOM-free game simulation.
 *
 * Placeholder skeleton for P0. Real systems (map grid, pathfinding, economy,
 * military) land in later phases per docs/PLAN.md workstream B.
 */
export const ENGINE_VERSION = '0.0.0';

/** Returns the engine package version. */
export function version(): string {
  return ENGINE_VERSION;
}
