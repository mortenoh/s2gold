/**
 * @s2gold/renderer — WebGL2 terrain + sprite renderer.
 *
 * Placeholder skeleton for P0. The terrain mesh, gouraud shading and sprite
 * batching land in later phases per docs/PLAN.md workstream C.
 */
export const RENDERER_VERSION = '0.0.0';

/** Returns the renderer package version. */
export function version(): string {
  return RENDERER_VERSION;
}
