{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window loads the embedded localhost server; IPC is limited to the app's own fullscreen/quit commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": ["core:default", "allow-toggle-fullscreen", "allow-quit"]
}
