{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No IPC: the window loads the embedded localhost server as an external URL.",
  "windows": ["main"],
  "permissions": []
}
